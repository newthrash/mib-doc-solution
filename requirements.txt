# Pinned for reproducibility; everything runs offline at runtime.
pymupdf==1.26.3
numpy==2.2.6
opencv-python-headless==4.11.0.86
# Second OCR engine: CNN line recognizer for merged-glyph rasters.
# Models ship inside the wheel, so runtime stays fully offline.
rapidocr-onnxruntime==1.4.4
onnxruntime==1.23.2
